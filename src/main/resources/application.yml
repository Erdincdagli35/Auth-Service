spring:
  jpa:
    show-sql: true

---

spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

  redis:
    host: ${REDISHOST:redis.railway.internal}
    port: ${REDISPORT:6379}
    password: ${REDISPASSWORD:koKJhmbyFYxlwpMGgbyCbxEYaBnDbwnT}
